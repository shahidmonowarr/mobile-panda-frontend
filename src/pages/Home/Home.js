import { Button } from '@mui/material';
import React from 'react';

const Home = () => {
    return (
        <div>
            <h1>This is Home</h1>
            <Button variant="contained">Outlined</Button>
        </div>
    );
};

export default Home;