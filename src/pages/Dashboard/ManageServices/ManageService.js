import React from 'react';
import { Outlet } from 'react-router-dom';

const ManageService = () => {
    return (
        <>
            <Outlet />
        </>
    );
};

export default ManageService;